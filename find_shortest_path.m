function [path_indexes, path_length] = find_shortest_path(points_x, points_y, n_iterations)
% Attempts to find the shortest path between a set of points (see the
% "traveling salesman" problem)

% TODO: monte-carlo solver

% TODO: implement approximate solver (choose first shortest path, then
% next...)


end